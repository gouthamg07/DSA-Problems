class Solution {
    public boolean containsDuplicate(int[] nums) {
        SetInteger set = new HashSet();
        for(int i=0;inums.length;i++){
            if(!set.add(nums[i])){
                return true;
            }
        }
        return false;
    }
}